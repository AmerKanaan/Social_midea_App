import 'package:flutter/material.dart';

import 'package:educational_world/main.dart';
class Log2 extends StatelessWidget {
  const Log2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return Scaffold(
      body: Container(
        child: InkWell(
          onTap: (){

          },
        ),
      ),
    );
  }
}
