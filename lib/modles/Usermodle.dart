import 'package:flutter/cupertino.dart';

class usermodle{

  String ? name;
  String ? email;
  String ? password;
  String ? c_password;
  String ? phone;
  String ? type;
  Text ? image;


  usermodle({
    this.name,
    this.email,
    this.password,
    this.c_password,
    this.phone,
    this.image,
    this.type,
  });
}